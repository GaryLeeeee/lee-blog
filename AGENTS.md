# 博客项目协作规则

## 规则边界

- 本文件只记录长期、稳定、会重复使用的项目约定。
- 一次性需求以当前任务说明为准；重复且步骤固定的工作，只有在用户明确要求时才封装为 Skill。
- 构建、启动和部署命令以`package.json`中的 scripts 为准，不在本文件重复维护。

## 项目结构

- 这是一个使用 Matery 主题的 Hexo 博客。
- 文章源文件位于`source/_posts/`，按主题放入已有子目录；Codex相关文章放入`source/_posts/codex/`。
- `source/_posts/博客导航.md`是站内导航，`README.md`是仓库首页目录。
- `_config.yml`是站点配置，`themes/matery/`是当前主题目录。
- `public/`、`db.json`、`.deploy_git/`和`node_modules/`属于生成结果、缓存或依赖，不手工修改。

## 文章规范

- 默认使用中文，表达简洁、通俗，少用没有必要的专业术语；先参考同目录文章的语气和结构。
- 新文章必须包含`title`、`date`、`tags`和`categories` Front Matter；`date`使用文章实际创建时间。
- 优先复用现有分类和标签。未经明确要求，不批量调整历史文章的分类、标签、文件名或发布日期。
- 内容以解决实际问题为主；案例和比喻用于帮助理解，不编造经历、数据或参考来源。
- 文章之间的站内引用优先使用 Hexo 的`post_link`标签，避免直接填写容易变化的文章路径。
- 外部资料使用可访问的原始链接，并集中放在文末“参考资料”中；引用观点时说明来源，不把推测写成事实。
- 修改现有文章时保留作者原有观点和结构，只改与当前需求直接相关的部分。

## 目录更新

- 新增文章时不自动修改目录，也不新增自动同步脚本或定时任务。
- 用户明确要求更新目录时，扫描当前文章，并同时更新`README.md`与`source/_posts/博客导航.md`。
- 两份目录中的分类、文章标题、链接和顺序保持一致；只更新相关分类，不顺手重排其他内容。
- 目录链接以站点实际生成的文章地址为准，不能只根据文件名猜测链接。

## 修改边界

- 只修改完成当前任务所需的文件，不做无关重构或全量格式化。
- 纯文章任务不修改主题、站点配置、依赖或部署配置。
- 未经明确要求，不新增依赖，不执行部署，也不修改远程仓库配置。
- 仓库中可能存在用户尚未提交的改动，必须保留并在其基础上继续工作。

## 验证与交付

- 文章、导航、站点配置或主题发生变化后，使用`package.json`中已有的构建脚本完成静态生成验证。
- 检查目标文章已生成，并确认 Front Matter、`post_link`和 Markdown 没有导致 Hexo 报错。
- 无法完成验证时，明确说明原因和未验证范围。
- 完成后简要说明修改文件、验证结果和仍需人工确认的内容。
